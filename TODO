- Check how diverging result handles is handled being followed by the
  "where" keyword.

- Make most of the rules hidden. Make some of them as supertypes.

- See if I can somehow use the "-ast-dump" in odin and the output of
  "tree-sitter parse" to see if tree-sitter-generated ast matches
  odin's.

- New inline asm.

- parse_value does not call parse_literal_value on open brace token,
  see if we should change anything in the grammar.

- Check if we allow for `-> (#simd[N]T)` style return types (commit
  ade003e12819849e6b167eeccfe44bca147ed0a5 in Odin).
